# Known bugs — running log

Quick-capture log for **one-off bugs discovered while doing other work**, so they get
recorded instead of derailing the task in hand. This is the home for "I noticed X is broken
but it's not what I'm here to fix."

**Process (refines the maintenance rule "surface adjacent issues, don't fix them inline"):**
when you find a bug that's out of scope for the current change —

1. **Do not fix it inline.** Stay on the current task.
2. **Append a row below** — date, one-line symptom, `file:line` (or area), and a one-line
   root-cause guess if you have one. Keep it terse; the point is to not get distracted.
3. **Tell the owner** in a one-line note (and/or drop a task chip) so it's visible.
4. For a bug we deliberately **park with a full analysis**, give it its own
   `docs/bug-<slug>.md` and link it from the row here.
5. When it's fixed, mark the row **DONE** (with the PR) or delete it.

| Date | Symptom | Where / root-cause guess | Status |
|---|---|---|---|
| 2026-07-21 | Ops quote tool flickers and blanks the price panel while typing a leg location | `api/src/routes/ops-ui.html` — background `render()` orphans the focused input; its delayed `blur` commits half-typed text → re-price → render → loop. Full report → [bug-ops-quote-typing-flicker.md](bug-ops-quote-typing-flicker.md) | **DONE** — PR #97 (guards) + PR #99 (morphdom diff-render); in prod via #101 |
| 2026-07-21 | Typed (not clicked) leg location never triggers auto-distance — "No distance" + `distCheck`, ops enter km by hand | `api/src/routes/ops-ui.html` — generic delegated `change` handler commits the location on blur before the per-input blur handler runs, so the blur handler sees no diff and skips `scheduleAutoDistance`. Full report → [bug-ops-quote-typed-location-no-autodistance.md](bug-ops-quote-typed-location-no-autodistance.md) | **DONE** — PR #103 (D3 fix, typed commit schedules auto-distance; regression spec `ops-typed-distance.spec.js`) |
| 2026-07-25 | `ops-chauffeur-date.spec.js:49` fails and `ops-addleg-date.spec.js:51` / `ops-autocomplete.spec.js:85` are flaky on clean `main` | `api/src/routes/ops-ui.html` — render race, NOT a date bomb (both date specs use dynamic dates since 7b93b4e). **The root cause below was DISPROVED on 2026-07-31 — do not re-walk it.** Original guess: "the leg-date `change` fires `render()`, which replaces the `Add leg` button between Playwright's mousedown and mouseup ~140 ms later; the delegated `[data-action]` handler then no-ops". That was written against pre-morphdom code — the diff-render shipped 2026-07-21, four days earlier, and patches nodes in place. Measured at 20x CPU throttle (CDP `Emulation.setCPUThrottlingRate`) with a deliberate 140 ms press: the button is NOT replaced (node identity survives), the click DOES reach the delegated handler, it resolves `addLeg`, and the leg IS created. A second hypothesis — that committing the date shifts the button so the click lands where it used to be — was also disproved: the button's y stays at 462 across 12 samples after the date commit. The 110 px shift visible in a naive before/after is just the new leg card appearing AFTER a successful click. **Repro (reliable, 5/8):** 6 busy-loop CPU burners + `npx playwright test ops-addleg-date --repeat-each=8 --workers=6 --retries=0`; the failure is `element(s) not found` for `.ch-leg` nth(1) — the second leg is never created. **Next probe:** instrument `addLeg`/`state.legs` inside the app under that load to tell whether the click is lost before the handler, or the handler runs and the render drops the leg | **DONE 2026-08-12 — neither.** The probe (leg-count MutationObserver + `pageerror` capture, CDP 20x throttle) showed the click always reaches the document, the `.ch-app` listener target is never swapped (`appSwaps: 0`), the button is inside it at click time — and the page throws `Cannot read properties of undefined (reading 'cents')`. **Root cause: the specs' own `**/admin/**` catch-all answers `{}` to the debounced `/admin/quote/estimate`.** The price pane reads `est.total.cents` unguarded, so an empty estimate throws inside `render()` — and `addLeg()` runs `refreshEstimate()` BEFORE `render()`, so the throw aborts the add and the new leg never leaves state for the DOM. Presents as a lost click, which is why both click hypotheses were dead ends. Fixed by giving every builder-driving spec a real estimate (`web-tests/e2e/_ops-estimate.js`): `ops-addleg-date` went 8/8 FAILED → 8/8 PASSED at `--repeat-each=8 --workers=6 --retries=0`; full suite 421 passed/4 failed → 435 passed/0 failed |
| 2026-07-25 | Live date bombs in the Playwright suite — will start failing ~2026-08-08 | `web-tests/e2e/plan-dates.spec.js:77-295`, `ops-itin-map.spec.js:125`, `date-correctness.spec.js:51,125` hard-code `2026-08-0x` literals. The `api/src/testSupport/dates.ts` helper migration (`nextIsoWeekday`/`futureIsoDate`) never reached `web-tests/`, which has no equivalent module | Open |
| 2026-07-25 | `<image-slot>` ignores author `aspect-ratio` — ~15 slots collapse to a 160px-tall letterbox instead of their intended box (e.g. `about-gal-*`, `about-team-*`, `blog-post-*`, `why-jetty`, `why-driver`) | `image-slot.js` `:host{...width:240px;height:160px}` — pages set `width:100%;aspect-ratio:…` inline but never override `height`, and an explicit height beats `aspect-ratio` (which only ever computes a *missing* dimension). Fix is either `height:auto` at each call site or dropping the fixed `height` from `:host` when an aspect-ratio is set | **DONE** — PR #166: `:host` now uses `width:240px;aspect-ratio:3/2;height:auto` so an author ratio drives the height; default box unchanged at 240x160. Guarded by `web-tests/e2e/image-slot-aspect-ratio.spec.js` (real layout) + a source-contract unit test |
| 2026-07-25 | `plan-dates.spec.js:244` flaky under parallel load — `.place-option` click fails "element was detached from the DOM" | **Root cause was NOT the render race guessed here.** Instrumenting the option's rect showed it sliding ~300px down over the first 200ms as the rail completes its first pricing pass, so Playwright never clears its "element is stable" check, and the menu is then rebuilt underneath it. A `plan.js` guard holding renders while a menu is open was tried and made it worse (renderPlaceMenu's own repaint closes the menu, which flushed the held render into the gap) — reverted. | **DONE** — PR #173: test waits for the rail to settle, then commits with Enter (a first-class app path) instead of a pointer click that must hit a moving target. 8/8 under induced load, was 0/3 |
| 2026-07-25 | Playwright e2e (200+ tests) gates nothing — it is not in `ci.yml`, whose 3 required checks are api tests, codegen parity, and web-tests **unit** only | `.github/workflows/ci.yml` — this is why #150 could rename `#pax` → `.pax-pill` and silently break 2 e2e tests from #147 on a green main. Owner chose 2026-07-25 to add e2e to CI as its own later PR | **DONE 2026-08-13 — PR #438**, which added the `web-tests e2e (offline)` job, advisory on arrival. **Still advisory ON PURPOSE, not by oversight** — owner decision 2026-08-15, reasoning and the trigger to flip it recorded in `go-live-checklist.md` ("AT CUTOVER: make the e2e job a required check"). Short version: the incidents this job exists for were stale tests rather than broken product, WordPress still serves customers, and ~8 min/PR across concurrent sessions is not worth it until this site is the live one. Its `--workers=2` cap was a mitigation for the flakiness whose root cause is fixed in the row above; raising it to 4 should be proven before the check is promoted |
| 2026-07-25 | `plan-dates.spec.js:157` fails on clean `main` — expects the over-10h hint to say /too much for one day/i, UI now says "more than is safe or enjoyable in one go" | Copy drift: the `#dates-drive-hint` wording changed without updating the assertion | **DONE** — no longer reproduces; the full e2e suite is green on `main` as of 2026-07-25. Resolved by other work (#160/#165), not by a change here |
| 2026-07-25 | `mobile-ux.spec.js:23` — tapping the terms-consent label does not tick `#agree`; fails **5 runs in 6** at `--workers=3`, passes reliably alone | Same render-race family as the two rows above. Ruled OUT by probe: the label centre resolves to the `<span>`, not the nested `target="_blank"` Terms link (`centreIsInsideAnchor: false`), and a single-run probe shows the click DOES toggle (`before:false → after:true`). Two speculative test fixes (link-free tap point; settling on `not.toBeChecked()` before clicking) did **not** move the rate — reverted rather than left in as unproven complexity. Needs the real render-race fix in `booking.js`, not a test tweak | Open |
| 2026-07-26 | `FakeTokenizedPaymentAdapter` has **no production guard** — a prod boot without a real tokenized adapter silently gives every Ride Board join a fake card that always "charges" successfully | `api/src/app.ts:85` defaults `paygw` to `new FakeTokenizedPaymentAdapter()`. The sibling `FakePaymentAdapter` (`adapters/payments.ts`) *does* throw when `NODE_ENV=production`; the tokenized one never got the same guard. Currently intentional (board is open to testers on fake payments), but it means the real-PayHere swap has no safety net — a misconfigured deploy fails open, silently. Found while writing `adapters/tokenizedPayments.test.ts` | Open |
| 2026-07-26 | Ride-board `charge()` accepts an **empty/unknown ref, and zero or negative amounts**, and reports success | `api/src/adapters/tokenizedPayments.ts` never validates the token or amount, and `services/rideBoardCutoff.ts:70` passes `ref: m.preapprovalRef ?? ''` — so a member row with a NULL preapproval ref charges an empty token, is marked `charged`, counted in `res.charged`, and emailed a confirmation. The `?? ''` fallback masks a data bug the fake can never surface | Open |
| 2026-07-26 | Ride-board `charge()` has **no idempotency** — the same `orderId` charges twice, with different txn ids | `api/src/adapters/tokenizedPayments.ts`. Protection today lives entirely in member-status bookkeeping in the cutoff sweep; the adapter seam offers none, so the future real PayHere adapter inherits a contract with no double-charge guard | Open |
| 2026-07-26 | Ride-board emails never contain the **public ride code** (e.g. `EM-4821`) — a traveller whose van was called off has nothing to quote and ops nothing to match on | `api/src/services/rideBoardEmails.ts` — `RideList.code` is not rendered by `sendRideConfirmed` / `sendRideCancelled` / `sendRideAtRisk`. Pinned by a self-cleaning `it.fails` at the bottom of `rideBoardEmails.test.ts`: it goes red the moment the code is added (drop the `.fails` then) | Open |
| 2026-07-26 | Ride-board emails interpolate `firstName` and place names into HTML **unescaped**, unlike every other sender | `api/src/services/rideBoardEmails.ts` has no `escapeHtml`; `services/notifications.ts` escapes customer-supplied text. `firstName` comes from Google sign-in so it is not attacker-controlled today — latent injection path if names ever become user-editable | Open |
| 2026-07-26 | Ride-board confirmed email shows only a **per-seat price**, never a total, for a multi-seat joiner | `api/src/services/rideBoardEmails.ts` — `RideMember.seats` can be up to 4, so someone who booked 3 seats sees `$24.00 per seat` and no `$72.00`. Possibly intended copy; flagged for an owner call | Open |
| 2026-07-26 | `RideListStatus` includes `'cancelled'` but **nothing in `src/` ever writes it**, and there is no ops-facing way to confirm or call off a van by hand — the cron sweep is the only status writer | `api/src/domain/rideList.ts` enum vs `services/rideBoardCutoff.ts`. Now that vans are visible in the ops Bookings queue (this change), someone will want to act on one; the read-only van sheet says so explicitly | Open |
| 2026-07-26 | Ride Board card: the rotated **"IT'S ON!" stamp overlaps long route titles** on phones — `Colombo Airport (CMB) → Sigiriya/Dambulla` renders with "Sigiriya" struck through by the stamp | `board.html` `.lcard .stamp{position:absolute;top:-13px;right:18px}` sits over `.lcard-route`, which wraps to 3 lines at 375px. Only bites confirmed lists with a long `from`/`to` pair. Noticed while doing the mobile compact-card pass; not touched there (out of scope) | Open |
| 2026-07-29 | `patch()` doesn't guard `deletedAt` like `update()`/`softDelete()` do — a notes-only PATCH can write 200 into a soft-deleted row | `api/src/db/postgresQuoteRepo.ts:351`, `api/src/db/quoteRepo.ts` — mostly unreachable (the route `get()`s first, which 404s on a deleted row, whenever `status`/`assignedTo` is present); narrow gap is an internal-notes-only PATCH on a row swept by the 24h abandoned-draft sweep. No money/lifecycle impact | Open |
| 2026-07-29 | Autosave comment overstates its own gate — `saveQuote` also silently no-ops on the autosave path for `chauffeurUndatedLegs()`, a second unpersisted-edits window the `beforeunload` guard doesn't cover either | `api/src/routes/ops-ui.html:3320-3322` — a chauffeur quote with an undated leg autosaves nothing and shows nothing; wording/behaviour worth an owner call | Open |
| 2026-07-30 | A **private** multi-leg quote's per-leg dates are lost on conversion to a booking — only the single booking date survives, so the ops drawer labels later legs "Leg 2"/"Leg 3" instead of dating them | `api/src/quote/quoteToBooking.ts` maps from `quote.request.engine`, and `PrivateLeg` is `{from,to,distanceKm}` with no date (`api/src/quote/types.ts:4`). The dates the operator typed live in the tool payload (`quote.request.tool.legs[].date`, `api/src/routes/internalQuote.ts:43`). Chauffeur days carry dates and are unaffected. Fixing it changes quoteToBooking's input contract — deliberately out of scope of the gaps/mark-paid plan | **DONE** 2026-08-08 — quoteToBooking now maps the TOOL payload's driving-leg dates per segment (stay days filtered out so indices line up, partial selections followed), and a date typed at booking time wins on the first leg |
| 2026-07-30 | A booking converted from a disconnected quote (legs that don't chain) keeps every place, but nothing downstream can tell a gap from a leg we drive — the ops drawer (`legsHtmlFor`, `api/src/routes/ops-ui.html`) and the customer itinerary email (`api/src/services/notifications.ts`) render every consecutive stop pair as a driven leg, so a gap (e.g. the customer's own Ella → Galle train) reads as a leg we drive | `api/src/domain/trip.ts` / `api/src/quote/quoteToBooking.ts` — `TripInput`'s flat `stops` array cannot express a gap; a per-segment `driven` flag was tried and cancelled (`trip_request` is a normalised table, not jsonb, so the flag was computed and thrown away on write). The real fix is the itinerary/leg/stay model already specified in `docs/backend-spec.md` §5.2 (owner decision 2026-07-30) | Open |
| 2026-07-30 | `api/drizzle/meta/` has **no `0029_snapshot.json`** (same gap as 0013 and 0023), so drizzle still believes the pre-0029 provider CHECK is current — the next `npm run db:generate` emits a redundant `DROP CONSTRAINT`/`ADD CONSTRAINT` for `payments_provider_supported` | Confirmed by running `drizzle-kit generate` against a throwaway copy of `drizzle/` + `schema.ts` (no DB): it wrote exactly that DROP/ADD pair, and numbered its snapshot `0030`, not `0029`. drizzle-kit has no snapshot-only command — the only ways to close the gap are a hand-written snapshot (wrong is worse than missing) or an extra empty migration. Harmless until someone regenerates; delete the redundant migration if it appears | Open |
| 2026-07-30 | `POST /admin/bookings/:id/mark-paid` inserts `amount: booking.amountDueNow ?? booking.total` with **no positive guard** — a $0 booking returns a cheerful 200 in memory and 23514s against `payments_amount_positive` in Postgres | `api/src/routes/admin.ts` — the in-memory payment repo enforces no CHECK, so no test can see it. Nothing produces a $0 booking today (the engine sets `amountDueNowCents = totalCents`), so it is latent; flagged during the mark-paid review | Open — **parked with the amount work**, see [deferred-manual-payment-amounts.md](deferred-manual-payment-amounts.md) |
| 2026-07-30 | Mark-paid can only ever record the **full amount due** — a cash deposit can't be entered, and overstated capture is what caps refunds | `api/src/routes/admin.ts` — no `amountCents` on the request (the schema is `.strict()`, so sending one 400s). Latent today because `amountDueNowCents = totalCents`. Full analysis → [deferred-manual-payment-amounts.md](deferred-manual-payment-amounts.md) | Open — **owner parked it 2026-07-30**, wants to do more work in this area first |
| 2026-07-30 | **Approve silently does nothing on the first press** right after reopening a quote — the blockers panel flashes "A price — the trip could not be costed" and self-heals within ~100ms, so the operator sees a dead button | `api/src/routes/ops-ui.html` — `reopenQuote()` nulls `lastEstimate` then calls `refreshEstimate()` async; a click inside that in-flight window trips `submitBlockers()`'s trailing price branch and `approveReady()` returns without transitioning. **Pre-existing on `origin/main`** (proved by swapping in main's `ops-ui.html` on a clean DB — identical 5 failures at the identical line). Reproduces as the 5 red specs in `web-tests/e2e/quote-tool.spec.js`, which CI never runs because they need `CH_E2E_API=1` | Open |
| 2026-07-30 | **CI never runs the `CH_E2E_API=1` Playwright suite**, so every spec needing a live API is skipped by the routine gate — two real defects sat red and unnoticed behind it | `.github/workflows/ci.yml` + `web-tests/package.json` `test:all`. Found when a `CH_E2E_API=1` run surfaced 5 failing `quote-tool.spec.js` specs the normal gate had always skipped. Worth an owner call on whether CI should stand up a DB for these | Open |
| 2026-07-30 | **mark-paid has no partial-payment concept** — it always records `amountDueNow ?? total` and always moves the booking to `paid` | `api/src/routes/admin.ts` — click it on a part-collection and the ledger overstates capture, the refund ceiling is inflated by the unpaid remainder, and the booking reads fully settled. Needs either an amount field or copy stating it means "paid in full". Owner call before launch | Open |
| 2026-07-30 | **Who marked a booking paid is recorded only in mutable free text** a lower-privileged role can erase | `api/src/routes/admin.ts` writes the actor into `ride_ops.ops_notes`; any `bookings:operate` holder (the `ops` role, which is denied `payments:act`) can wipe it via `POST /admin/ops/bookings/:id/flags`. SH8 gave manual refunds immutable `requested_by`/`confirmed_by` columns; manual capture shipped without the equivalent. `payments` has no actor column | Open |
| 2026-07-30 | **The refund UI still instructs "complete the refund in the PayHere dashboard" and requires a PayHere ref** — but cash bookings are now refundable | `api/src/routes/ops-ui.html` refund block + `api/src/db/refundRepo.ts` (`gateway_ref_required`). Recording manual payments was justified by refundability, so this is the untouched consumer. Customer-facing refund email is method-agnostic, so nothing wrong reaches the customer | Open |
| 2026-07-30 | **Marking a booking paid enrols the customer in the automated email schedule for the first time** — 48h trip reminder + post-trip review request | `api/src/services/scheduler.ts` (`TRAVELLED_STATUSES` includes `paid`). Previously `payment_pending → paid` was reachable only from the PayHere webhook, so cash customers never matched. mark-paid itself sends nothing (owner decision), but the reminder opens with "here are the details again" while being the customer's first ever email. Owner call | Open |
| 2026-07-30 | **CI runs zero Playwright specs of any kind** — broader than the earlier `CH_E2E_API` row | `.github/workflows/ci.yml` has three jobs and never invokes `playwright test`. So even the offline, non-gated specs only run when a developer runs `npm run test:all` locally, and the API-backed ones need `CH_E2E_API=1` plus a local Postgres on top. The strongest end-to-end coverage on this branch gates nothing | **PARTLY DONE 2026-08-13** — the offline suite now runs in CI (PR #438, advisory). The `CH_E2E_API=1` specs still do not: they need Postgres stood up in the workflow and carry their own known failures (the 2026-07-30 approve-first-press row), so gating them would import a red suite. Still open as its own piece of work |
| 2026-07-30 | `markSucceededManually` has no status precondition — it would flip a `failed` payment to `succeeded` | `api/src/db/paymentRepo.ts`, `api/src/db/postgresPaymentRepo.ts`. Unreachable from the mark-paid route as written, but it is now part of the `PaymentRepo` interface with no state guard, unlike the settlement repo | Open |
| 2026-07-30 | The mark-paid confirm dialog shows `booking.total` while the server records `amountDueNow` | `api/src/routes/ops-ui.html` vs `api/src/routes/admin.ts`. Identical today (`amountDueNowCents === totalCents` for every product), diverges the moment deposits ship — the operator would confirm the full fare while the ledger books the deposit, and the booking would still go to `paid` | Open |
| 2026-07-30 | Abandoned unpriced shells show the queue's red "urgent" age chip from hour 6 until the 24h sweep, and inflate the queue count and "In progress" filter | `api/src/routes/ops-ui.html` `quoteAgeTone` / row counts. Urgent is the queue's act-on-this signal; shells are the one row class needing no action. Consider treating `unpriced` like a done state for ageing | Open |
| 2026-07-31 | Historical rides finished in ops (`ride_ops.fulfilment_status='completed'`) still read `booking.status='paid'` — nothing wrote past `paid` before the lifecycle mirror shipped | `api/src/routes/ops.ts` — the ops UI is UNAFFECTED (stage derives from ride_ops), but `GET /admin/bookings?status=completed` misses them and the daily digest's Confirmed count starts from zero. Backfill is optional and must be SQL: `paid → completed` is deliberately illegal in `assertTransition`. Size it with `api/scripts/promote-audit.ts` (`bookings_vs_fulfilment`) | Open — owner call, no correctness impact |
| 2026-07-31 | Quotes behind already-cancelled/refunded bookings stay `won`, so founder Funnel `wonValue` overstates HISTORY | `api/src/services/quoteOutcome.ts` only corrects NEW cancels/refunds. Restating history is an owner call (it changes numbers already reported) | Open — owner call |
| 2026-07-31 | **44 sent quotes (~85% of the live pipeline) were 12 days from silently auto-expiring**, and `expired` was a terminal state — the team could never have revised and re-sent them | `api/src/services/quoteExpiry.ts` TTL assumed 'sent' = "customer went quiet"; the team uses it as a working state while the site is dark. Found by `promote-audit.ts` against prod: oldest sent quote 18 days, TTL 30, cron live daily at 06:00 UTC | **DONE** — TTL 30→180 days, `expired → draft` now legal (drops the frozen rate card, clears `decidedAt`) |

| 2026-07-31 | **`mobile-ux.spec.js` "mobile terms consent has a reliable touch target" is ~50% flaky on untouched code** — clicking `label:has(#agree)` at 390px intermittently fails to toggle the checkbox (the geometry assertions above it always pass) | `web-tests/e2e/mobile-ux.spec.js:23` vs `booking.html` `.terms-consent`. Measured over 20 serialized runs (`--workers=1 --retries=0`) at `6957e12`, i.e. before any motion work: **10/20 pass**. An earlier note here claimed it simply "fails on main" — that was read off a single run and was wrong; it is a coin flip, not a hard failure. Separately, the step-panel entrance added in #218 drove it to **0/20** by translating every tap target inside the panel for 260ms; that regression is fixed (panel now cross-fades only, back to 11-13/20). The remaining ~50% flake is the original defect and still unexplained — worth its own session, since the assertion is that a traveller can tap the label to accept terms on a phone. Consent is also enforced server-side and by `goBook`, so a booking cannot skip terms | Open |
| 2026-07-31 | **The total on screen and the total written to the database can differ** — measured $75.00 in the builder vs $266.00 stored. The operator saves at a price they never saw, and the queue row shows the stored one | `api/src/routes/ops-ui.html` `_runEstimate` deliberately prices only the READY legs when a leg has no distance yet (Defect A partial pricing, banner: "Covers the ready legs"), but `savePayload()` always sends the FULL itinerary and `internalQuote.ts` `resolveAndPrice` resolves the missing distances server-side and prices all of them. Nothing reconciles the two: `/save` returns only `{id, reference, status}`, so the builder never learns the stored total. `submitBlockers()` gates on `legKm(l) > 0`, so this cannot reach a customer — the damage is a queue row that disagrees with the builder, and a silent save at an unseen price. Reproduced with a client-unresolvable place; the resolved km IS persisted into `request.tool`, so a reopen self-heals. Proposed fix: return the resolved legs + priced total from `/save` and have `saveQuote()` adopt them, so the two agree by construction and the operator gets the resolved distances without a reopen | Open |
| 2026-08-01 | **A pay-link customer is returned to the booking FORM after paying, not a confirmation** | `api/src/server.ts` sets `returnUrl`/`cancelUrl` ONCE globally to `${APP_BASE_URL}/booking.html`, and `adapters/payhere.ts` `createCheckout` has no per-payment override (`return_url: this.opts.returnUrl`). A customer who arrives via a pay link never touched the booking wizard, so the page they land on after a successful payment is a form for a trip they have already bought. Worse if `APP_BASE_URL` is on a host that does not serve `booking.html` at all — then it is a dead end straight after taking money. Needs an owner call on the destination (the manage page for that booking is the obvious candidate, since the token already exists) before it can be built | Open |
| 2026-08-01 | **`pay.ceylonhop.com/` serves the ops dashboard login at its root** | `api/src/app.ts` mounts `opsUiRoutes` at `'/'` as a bare-root alias so `ops.ceylonhop.com` serves the tool directly — but every custom domain on the service inherits it, so a customer who trims a pay link back to the domain lands on the internal tool's sign-in. Auth-gated, so not a data leak, but it is an internal surface on a customer domain. Fix is host-aware routing at the root (or a redirect to the site), which is a real change, not a config tweak | Open |
| 2026-08-01 | **Prod PayHere live checkout fails `PH-0013 Unauthorized payment request`** | First live-mode attempt, 2026-08-01, USD 50.00 on `pay.ceylonhop.com`. The merchant resolved (modal showed "Ceylon Hop (PVT) LTD"), so `merchant_id` is right and the domain served the checkout. The hash in `adapters/payhere.ts` is the standard `md5(merchant_id + order_id + amount + currency + md5(secret))` with `amount` as `toFixed(2)` — code looks correct, so this is account config. Ranked candidates: (1) `PAYHERE_MERCHANT_SECRET` on Render is not the live secret for that account (a new key was generated the same day; a stale value or pasted whitespace does exactly this); (2) **USD is not enabled on the account** — the rate card hardcodes `currency: 'USD'`, and switching to LKR would touch pricing, emails and every customer-facing total; (3) live domain not approved (checklist says PayHere is apex-only). PayHere's dashboard logs the rejection reason and will name which. **RESOLVED: it was (3), the domain — and PayHere then approved the subdomain.** | **DONE 2026-08-02 — real money settles on `pay.ceylonhop.com`.** A $39 card payment completed end to end: booking `CH-RKWNW`, PayHere payment `320048261124`, settled via the webhook. Candidates (1) the secret and (2) USD-not-enabled were both wrong — USD is fine and needs no LKR migration. **Do not re-derive the apex-only wall**: it was real on 2026-08-01 and came down the next day. This row sat reading "Open — blocks taking any real payment" for six days while production was trading, which is how a reader (and an agent, 2026-08-08) loses half an hour concluding the business cannot take money. |
| 2026-08-02 | **A booking-detail fetch that fails during cancel is swallowed with no beacon**, and the error that *is* reported names `refreshRow`, hiding that a cancellation was in flight | `api/src/routes/ops-ui.html:3032` — the cancel handler runs `state.detailData=await api.get('/bookings/'+id).catch(()=>state.detailData)` and then `await refreshRow(id)`. Only the second call is instrumented, so when the API is unreachable the first failure leaves no trace at all, and the alert email reads `[ops-ui] refreshRow — Failed to fetch` with nothing to say a booking was being cancelled. Seen in prod at `2026-08-02T05:44:43Z`: the cancel POST succeeded (status written, seats released, customer emailed), the board refresh did not, and the operator got a "Refresh failed" toast on a board still showing the booking live — so they may not know the cancel landed. Retrying is safe (`cancelled → cancelled` is not in `ALLOWED`, `api/src/domain/status.ts:24`, so a second press 409s before the email). Fix is a beacon on the swallowed `catch` plus an operation-named context (e.g. `booking cancel → refreshRow`) so the alert identifies the operation, not the helper | Open |
| 2026-08-03 | **A website payer who corrects a declined billing address creates a SECOND draft booking** rather than updating the first | `booking.js` `createApiBooking` — the idempotency key is `hash(body)`, and `billing` is now part of the body, so changing one character of the address mints a new key and the server creates a new booking. Deliberate: excluding billing from the key would make the idempotent create return the EXISTING row with the OLD billing, i.e. charge the corrected payer against details they already fixed — the CH-RKWNW failure (#279) exactly. The cost is that a decline-then-correct leaves one abandoned booking, in `payment_pending` if the first attempt reached the gateway. The right fix mirrors #274 on the website side: a re-record path that updates the payer/billing on the existing booking instead of creating another | Open — owner call, safe by default |
| 2026-08-03 | **The daily scheduled-notifications tick has NEVER run in production — 42 of 42 GitHub Actions runs have failed (re-verified 2026-08-08) since the workflow landed on 2026-06-28**, all with `curl: (22) ... error: 401` | The repo secret `ADMIN_API_KEY` was never set (`gh secret list` returns nothing), so `.github/workflows/notifications.yml` sends a literally empty `x-admin-key:` header and `lib/opsMiddleware.ts:51` fail-closes (it requires the header AND a non-empty configured key to match). Everything riding that one tick in `routes/admin.ts:485` has therefore never fired in prod: **pre-trip reminder + review-request emails (M14), the Ride Board cutoff (confirm/charge or expire due lists), quote expiry, the abandoned autosave-shell sweep, the stale shared-hold sweep, and the daily ops digest (M17)**. Not a code bug — the workflow's own header comment predicted exactly this failure mode. Fix is owner-only: set the repo secret to match `ADMIN_API_KEY` in the `ceylon-hop-api` Render env, and confirm it is set THERE too (an empty value on either side 401s identically; prod logs a startup warning, `server.ts:33`). Related: the payment-recovery watchdog (`POST /admin/jobs/watchdog`, expected every ~15 min) has no scheduler at all — still an unchecked box in `go-live-checklist.md:94`, blocked on the same key | Open |
| 2026-08-04 | **A re-book applies the corrected CUSTOMER but silently ignores the trip fields** — vehicle, pax, bags, date and time | `api/src/routes/internalQuote.ts` `/:id/book`. The re-book path calls `refreshPayerDetails`, which rewrites the customer (and billing) only; the rest of the modal feeds `input`, which is built by `quoteToBooking` at CREATE time and never rewritten. So an operator fixing a wrong travel date on a re-book gets a success toast and no change. Narrower version of the bug fixed today, and left deliberately: rewriting `input` means rewriting the normalised `trip_request` rows, which is a real change rather than a fix. Either apply them or say they weren't applied — a silent drop is the thing we just removed | Open |
| 2026-08-04 | **A quote→booking conversion cannot be undone**, so a test booking permanently claims a real quote | `quotes.converted_booking_id` is written by `/:id/book` and cleared nowhere — the only assignment to null is on a brand-new quote. Cancelling the booking does not release it either, because the link lives on the quote, not the booking. Q-UD8A9 got into exactly this state (test-booked 1 Aug, then needed for a real customer). Today's fix makes the common case recoverable — an unpaid booking now takes the corrected customer — but a quote whose booking is PAID, cancelled or refunded can never be booked correctly through the UI. Needs an ops action to release a quote from its booking: founder-only, reason required, refused once paid, same shape as the cancel/refund rules | Open |
| 2026-08-04 | A **gapped partial-link booking** (middle leg dropped) still renders the gap pair as a driven leg in the ops drawer and the customer itinerary email — the coverage sentence added alongside contradicts it rather than removing it | `api/src/domain/trip.ts` flat `stops` cannot express a gap (same root cause as the 2026-07-30 row above); partial links make gaps routine instead of rare. Real fix is the itinerary/leg/stay model, `docs/backend-spec.md` §5.2 | Open |
| 2026-08-04 | **`ops-mark-paid.spec.js` fails whenever the `CH_E2E_API=1` tier actually runs** — `ReferenceError: futureIsoDate is not defined` | `web-tests/e2e/ops-mark-paid.spec.js:43` — the fixture builder calls `futureIsoDate(45)` INSIDE a `page.evaluate` callback, which executes in the browser where the Node-side helper import does not exist. Dormant because the default suite skips the spec (`CH_E2E_API !== '1'`); reproduced identically on unmodified `main`, so it is a harness bug in the spec, not a UI regression. Fix is computing the date outside the evaluate and passing it in as an argument | Open |
| 2026-08-07 | **`manage.html` still checks out through the PayHere iframe SDK** — the exact flow `pay.html` was redesigned away from after CH-XKZL3 | `manage.html` loads `https://www.payhere.lk/lib/payhere.js` and calls `payhere.startPayment` with `onCompleted`/`onDismissed`/`onError`. Those callbacks are unreachable when a failure happens inside the cross-origin iframe (the whole finding of `docs/checkout-redirect-spec.md`), so a customer paying a balance from a confirmation email can still land on a spinner that never resolves, with a `pending` row and no webhook. `pay.html`'s top-level form POST is the fixed pattern to port. Noticed while instrumenting the page for analytics — the SDK callbacks are now tracked, which will make the dead-end *visible* but does not fix it | Open |
| 2026-08-07 | **The privacy policy promises a consent banner that pay/quote no longer show** — it says analytics are "off by default until you accept them in the cookie banner", but `pay.ceylonhop.com` and `quote.ceylonhop.com` now grant analytics on arrival with no banner (owner call, `ASK_FIRST = false` in `consent-transactional.js`) | `tools/legal/privacy.body.html`, the analytics bullet → regenerates to `privacy.html`. Compliance, not a code fault: the legitimate-interest basis for not asking depends on the policy describing what we actually do, and right now it describes the opposite. Ads ARE still denied on both pages, so only the "off by default / cookie banner" clause is wrong. Suggested wording in `docs/analytics/property-tracking.md` §2.2 — owner's words, then `npm run generate:static`. Deferred by owner 2026-08-07 | FIXED 2026-08-16 — the banner was removed site-wide and the privacy bullet rewritten to match (no banner, analytics granted by default, no advertising cookies) |
| 2026-08-08 | **Six e2e specs fail on clean `main`** — `ops-assign-picker-consistency:94,124`, `ops-cancel-refund:62`, `ops-refund-workflow:172,263` (plus `ops-chauffeur-date:49`, already rowed above) | Confirmed by stashing all local work and running them against pristine `origin/main` at `d405917`, so none are caused by the route-variant map fix that found them. Separately, `map-expand` and `ops-map-pin-numbers` were failing for a harness reason and are FIXED here: their Google stubs lacked `getZoom`/`addListener`, which pins started calling when they became zoom-aware (2026-08-07), so the pin pass threw into its "markers are non-essential" catch and four tests asserted against an empty marker list. **The three refund specs are diagnosed and FIXED (2026-08-08): stale specs, not product bugs, not a state leak.** #357 introduced the inline `#refundreason` field whose id `ops-cancel-refund:62` still asserted absent (a leftover from the `#reversereason` era), and #360 made a founder's press fire PayHere immediately, so the `manual_pending` row and separate `refundexecute` press that `ops-refund-workflow:172,263` waited on stopped persisting. Both PRs updated unit tests but not these e2e specs, and CI runs no Playwright (rows above), so they merged red. Specs re-pointed at the current flow: 62 pins the #357 layout, 172 pins the API-unavailable→manual-evidence fallback, 263 pins one-press→`api_confirmed` | Open — `ops-assign-picker-consistency:94,124` still uninvestigated (passed 2026-08-08 full run, so likely load-sensitive); pin tests + 3 refund specs fixed |
| 2026-08-08 | **`customerPages.test.ts` "NEVER puts the amount in the preview" fails at random (~1 in 1,750 CI runs)** — `AssertionError: expected '<!doctype html>...' not to contain '458'`, and a re-run of the identical commit passes (seen live: run 31288764988 on `bf6b3d8`) | `api/src/routes/customerPages.test.ts:165,273` — the fixture prices at `totalCents: 45800` and asserts the `<head>` lacks the bare digit run `458`, but the head embeds the base64url link token three times (`og:image`, `og:url`, `twitter:image`), ~150 characters of uniform entropy inside the scanned region. Any token containing `458` trips it. Measured over 500k signings: 1 in 7,353 pay tokens and 1 in 6,250 view tokens, 1 in 4,762 for the pair. Nothing wrong with the product — the card genuinely never carries a price | **DONE** — the two assertions now strip token query VALUES (`withoutLinkTokens`, `[?&]t=` only, markup untouched) before scanning; `not.toContain('$')` kept. Pinned by a new test using a real colliding token (`…x458qg`), which is red without the strip |
| 2026-08-09 | **The confirmation email's "View your booking" button goes to the wrong link** — owner-reported against a real PAID booking (CH-NFRAL) | `api/src/services/notifications.ts` `manageButton()` renders `manageUrl(booking, baseUrl, secret)` → `${baseUrl}/manage.html?t=<signed token>`, where `baseUrl` is `deps.bookingBaseUrl ?? config.APP_BASE_URL` (`api/src/app.ts:387`). **Which** wrong URL it opens was never captured, and that single fact decides everything: an `APP_BASE_URL` pointing at the WordPress apex (serves no `manage.html`), at an ops/API host, or at the `localhost:4173` default is a Render config change and the owner's call; reaching the right host and failing there is a code/token bug. Note the same URL ships in SIX emails (confirmation, deposit received, details needed, payment failed, payment incomplete, trip reminder), and `APP_BASE_URL` also drives PayHere's `return_url`/`cancel_url` — repointing it drags all of that along, which is exactly why `PAY_BASE_URL` was split out on 2026-07-31. A `MANAGE_BASE_URL` in that same shape is the lever if it turns out to be a host problem. Related: the 2026-08-01 row above (`APP_BASE_URL` on a host that serves no `booking.html`) and the 2026-08-07 `manage.html` PayHere-iframe row | Open — **parked by owner 2026-08-09** to be settled together with a `manage.html` redesign, since the link and the page it lands on are one decision. Ask which URL it opens first |
| 2026-08-12 | **`ops-leg-card-layout.spec.js` "the actions-only toolbelt collapses at rest and expands on hover" — `card.hover()` leaves the tools row at height 0** | `api/src/routes/ops-ui.html` (`.ch-leg-tools` / `.is-actions-only`) or the spec's hover point. NOT flake and NOT the estimate-stub bug fixed the same day: it fails **4/4** at `--retries=0`, solo, at `--workers=1`, both before and after that fix — it only reads as "flaky" in a normal run because `retries: 1` lets it pass on the second attempt. The collapse half passes (height < 1 at rest); only the hover expansion fails, so the row never re-expands. Found while fixing the ops e2e flakiness; deliberately not fixed there — separate defect, and possibly a real user-facing hover bug rather than a test one | **DONE 2026-08-14 — it took two independent fixes, and neither was sufficient alone.** The expansion is animated, so a starved animation frame under load left the height poll reading 0: fixed by `reducedMotion: 'reduce'` on this spec (landed on `main` separately while this was in flight). That alone took it from 4/4 failing to 2/4. The rest was the empty-estimate throw in the row above. With both, `--repeat-each=4 --workers=6 --retries=0` runs **16/16**. It was briefly quarantined with `test.fixme` on the strength of the first half of that picture; the quarantine is removed, since skipping a mostly-passing test is worse than the flake was. **NOT fully closed:** in a FULL run it still fails at its third phase — `row.classList.remove('is-actions-only')` then a 350ms wait and a 3s poll. That class is derived from state, so a morphdom re-render inside the window restores it and the row collapses to 0. The test toggles the class to dodge the re-render race on a real fee click and inherited a different one; the durable fix is to drive a real fee (which the estimate fix may now have made reliable) or to assert against something the app does not own. Left for whoever owns this spec | Open — narrowed |
| 2026-08-25 | **Every live Ride Board card approval failed `PH-0014 Unauthorized payment request`** — and PayHere's PUBLISHED DOCS ARE WRONG about why | The Preapproval API hashes over the amount PayHere will authorize. Their docs (and sandbox) say `1.01` for non-LKR when no `amount` is sent, which is what `api/src/adapters/payhereTokenized.ts` used — but their live preapproval page was quoting **USD 0.51**, and that mismatch was the whole bug. PayHere support, 2026-08-24: *"Please use 0.51 as the authorization amount for other currencies when generating the hash value. For the PayHere Preapproval API, the authorization amount has been changed in the PayHere Live environment. This change will be reflected in the PayHere documentation soon."* **Three plausible theories were all WRONG** and cost a day between them — (1) `prod.ceylonhop.com` not being an approved PayHere domain (the 2026-08-01 `PH-0013` precedent, which really was a domain), (2) PayHere issuing a merchant secret PER DOMAIN so the board was signing with `pay.ceylonhop.com`'s, (3) Automated Charging never being approved on the account. Do not re-derive them: **ask PayHere for the logged rejection reason first**, it is one email and it ends the guessing. Also note `api/.env` holds a SANDBOX merchant id (7 digits starting `12`); prod is `243025`, so a mismatch there is expected, not a bug | **DONE 2026-08-25 — #585.** `hashAmount` is now mode-dependent: live non-LKR `0.51`, sandbox still `1.01` (docs still say 1.01 and sandbox still expects it), LKR unchanged at `10.00`. **Revisit when PayHere updates its documentation** — if sandbox moves to 0.51 too, the branch can collapse to one constant |
